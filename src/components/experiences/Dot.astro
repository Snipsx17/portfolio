<div class="relative w-[40px] h-[40px] z-10">
    <div class="absolute w-6 h-6 top-5 left-5 bg-blue rounded-full"></div>
    <div class="lds-ripple"><div></div><div></div></div>
</div>
<style>
    .lds-ripple,
    .lds-ripple div {
      box-sizing: border-box;
    }
    .lds-ripple {
      display: inline-block;
      position: relative;
      width: 40px;
      height: 40px;
    }
    .lds-ripple div {
      position: absolute;
      border: 2px solid #0029FF;
      opacity: 1;
      border-radius: 50%;
      animation: lds-ripple 1s cubic-bezier(0, 0.2, 0.8, 1) infinite;
    }
    .lds-ripple div:nth-child(2) {
      animation-delay: -0.5s;
    }
    @keyframes lds-ripple {
      0% {
        top: 16px;
        left: 16px;
        width: 8px;
        height: 8px;
        opacity: 0;
      }
      4.9% {
        top: 16px;
        left: 16px;
        width: 8px;
        height: 8px;
        opacity: 0;
      }
      5% {
        top: 16px;
        left: 16px;
        width: 8px;
        height: 8px;
        opacity: 1;
      }
      100% {
        top: 0;
        left: 0;
        width: 40px;
        height: 40px;
        opacity: 0;
      }
    }
</style>